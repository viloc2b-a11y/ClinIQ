# Scripts

One-off maintenance and automation (data backfills, codegen, local tooling).

Prefer `npm run …` entries in root `package.json` for scripts developers run often; use this folder for longer or ad-hoc Node/TS scripts.
